[package]
name = "diesel-attributes"
version = "0.1.0"
edition = "2024"

[dependencies]
quote = {version = "1.0", features=["proc-macro"]}
syn = {version = "2.0", features=["full"]}

[lib]
proc-macro = true
